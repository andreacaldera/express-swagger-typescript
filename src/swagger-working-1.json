{
  "openapi": "3.0.3",
  "info": {
    "title": "Compliance internal api",
    "description": "Compliance internal api.",
    "version": "1.0.0"
  },
  "paths": {
    "/internal/api/individual/referenceNumber/{referenceNumber}": {
      "get": {
        "summary": "Returns an individual",
        "description": "Returns individual profile based on reference number",
        "responses": {
          "200": {
            "description": "Individual response"
          }
        }
      },
      "parameters": [
        {
          "name": "referenceNumber",
          "in": "path",
          "description": "Individual reference number",
          "required": true,
          "schema": {
            "type": "integer",
            "format": "int32"
          }
        }
      ]
    },
    "/api/entity/{id}": {
      "get": {
        "summary": "summary todo",
        "description": "description todo",
        "responses": {
          "200": {
            "description": "response description todo"
          }
        }
      },
      "parameters": [
        {
          "name": "id",
          "in": "path",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ]
    },
    "/api/person": {
      "post": {
        "summary": "summary todo",
        "description": "description todo",
        "responses": {
          "200": {
            "description": "response description todo"
          }
        },
        "requestBody": {
          "description": "request body todo",
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#components.schemas.Person"
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Address": {
        "properties": {
          "city": {
            "type": "string"
          },
          "postCode": {
            "type": "string"
          },
          "countryIsoCode": {
            "type": "string"
          }
        },
        "required": ["city", "postCode", "countryIsoCode"],
        "additionalProperties": false,
        "type": "object"
      },
      "Person": {
        "properties": {
          "id": {
            "type": "number"
          },
          "firstName": {
            "type": "string"
          },
          "lastName1": {
            "type": "string"
          },
          "address": {
            "items": {
              "$ref": "#/components/schemas/Address"
            },
            "type": "array"
          }
        },
        "required": ["id", "firstName", "lastName1", "address"],
        "additionalProperties": false,
        "type": "object"
      }
    }
  }
}
